Label loc generation is one off currently ( the interrupt handler is set one before )
Label loc genration is only off on int-handler at the moment, it seems... maybe earlier

Get .d" working (should be like .", but only output if debug-mode is non0

Test clock

Implement screen using SDL?

Allow for MEM locations to take a label on compiling
